const initialState = {
  projects: [
    {
      name: 'Quizlet ionic client'
    },
    {
      name: 'Watch the Squirrel'
    }
  ]
}

export default (state = initialState, action) => {
  return state
}
