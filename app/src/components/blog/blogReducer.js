const initialState = {
  blogPosts: [
    {
      title: 'Ionic 3'
    },
    {
      title: 'Create react native'
    }
  ]
}

export default (state = initialState, action) => {
  return state
}
